import 'package:flutter/material.dart';

class MoneyManagment extends StatelessWidget {
  const MoneyManagment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
